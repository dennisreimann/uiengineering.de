const breakpoints = require('../../../../uiengine/tokens/breakpoints')
const spaces = require('../../../../uiengine/tokens/spaces')

module.exports = {
  title: 'Werte und Einheiten',
  tokens: [
    ...breakpoints,
    ...spaces
  ]
}
